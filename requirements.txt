# EchoLens — analysis and figure reproduction.
#
# Enough to run notebooks 01-03 and 07-09: the SMD pipeline, every figure under
# paper/figures/smd*, and every table under paper/tables/smd.
# Pinned to the versions that produced the published results.
#
# To re-run model inference, ASR, or speech metrics, also install
# requirements-inference.txt.

pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0
matplotlib==3.9.2
seaborn==0.13.2
jupyterlab>=4.0
